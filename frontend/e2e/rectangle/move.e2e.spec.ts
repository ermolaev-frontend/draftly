import { test } from '@playwright/test';

import { moveFrom } from './rectangle.helpers';

test.describe('Move rectangle', () => {
  test('should move rectangle up', async ({ page }) => {
    // TODO: реализовать шаги для перемещения вверх
  });
  test('should move rectangle down', async ({ page }) => {
    // TODO: реализовать шаги для перемещения вниз
  });
  test('should move rectangle left', async ({ page }) => {
    // TODO: реализовать шаги для перемещения влево
  });
  test('should move rectangle right', async ({ page }) => {
    // TODO: реализовать шаги для перемещения вправо
  });
}); 