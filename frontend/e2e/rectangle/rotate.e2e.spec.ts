import { test } from '@playwright/test';

import { rotateHandle } from './rectangle.helpers';

test('should rotate rectangle 30 degrees right', async ({ page }) => {
  // TODO: реализовать шаги для поворота вправо
});

test('should rotate rectangle 30 degrees left', async ({ page }) => {
  // TODO: реализовать шаги для поворота влево
}); 